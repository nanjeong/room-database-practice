<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="detailViewModel"
            type="com.example.myapplicationtracker.detail.DetailViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/weight"
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:layout_marginLeft="15dp"
            android:background="@drawable/weight_view_background"
            android:gravity="center"
            android:textSize="35sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0.4"
            app:setWeight="@{detailViewModel.weight}"
            tools:text="50" />

        <TextView
            android:id="@+id/date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/string_color"
            android:textSize="22sp"
            app:layout_constraintBottom_toTopOf="@id/compare"
            app:layout_constraintStart_toEndOf="@id/weight"
            app:layout_constraintTop_toTopOf="@id/weight"
            app:setDate="@{detailViewModel.weight}"
            tools:text="05/11" />

        <TextView
            android:id="@+id/compare"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#2979ff"
            android:textSize="23sp"
            app:layout_constraintBottom_toTopOf="@id/fighting_string"
            app:layout_constraintStart_toEndOf="@id/weight"
            app:layout_constraintTop_toBottomOf="@id/date"
            tools:text="+0.2" />

        <TextView
            android:id="@+id/fighting_string"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{@string/cheering_phrase(detailViewModel.phrase)}"
            android:textColor="@color/string_color"
            android:textSize="22sp"
            android:textStyle="italic"
            app:layout_constraintBottom_toBottomOf="@id/weight"
            app:layout_constraintStart_toEndOf="@id/weight"
            app:layout_constraintTop_toBottomOf="@id/compare"
            tools:text="Keep Going!! yeah" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>