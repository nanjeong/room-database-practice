<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" android:id="@+id/nav_graph"
    app:startDestination="@id/todayWeightFragment">

    <fragment
        android:id="@+id/todayWeightFragment"
        android:name="com.example.myapplicationtracker.weighttracker.TodayWeightFragment"
        android:label="TodayWeightFragment" >
        <action
            android:id="@+id/action_todayWeightFragment_to_detailFragment"
            app:destination="@id/detailFragment" />
    </fragment>
    <fragment
        android:id="@+id/detailFragment"
        android:name="com.example.myapplicationtracker.detail.DetailFragment"
        android:label="DetailFragment" >
        <argument
            android:name="id"
            app:argType="long" />
    </fragment>
</navigation>